apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  selector:
    app: myapp          # routes to Pods carrying this label
  ports:
    - port: 80
      targetPort: 8000  # the container's port
  type: ClusterIP
